---
name: Docker Build Action

on:
  push:
    branches:
      - main

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
    gardener:
        name: Gardener Build
        runs-on: ubuntu-latest

        steps:
          - name: Log in to the container registry
            uses: docker/login-action@v4
            with:
              registry: ${{ env.REGISTRY }}
              username: ${{ secrets.DOCKER_REGISTRY_USER }}
              password: ${{ secrets.DOCKER_REGISTRY_TOKEN }}

          - name: Checkout
            uses: actions/checkout@v6

          - name: set gardener version
            run: echo "BUILD_BRANCH=v1.132-image-defaulting" >> $GITHUB_ENV

          - name: build gardener images
            run: gardener/build.sh

    mcm:
        name: Machine Controller Manager Build
        runs-on: ubuntu-latest

        steps:
          - name: Log in to the container registry
            uses: docker/login-action@v4
            with:
              registry: ${{ env.REGISTRY }}
              username: ${{ secrets.DOCKER_REGISTRY_USER }}
              password: ${{ secrets.DOCKER_REGISTRY_TOKEN }}

          - name: Checkout
            uses: actions/checkout@v6

          - name: set gardener version
            run: echo "BUILD_BRANCH=support-ignition" >> $GITHUB_ENV

          - name: build gardener images
            run: machine-controller-manager/build.sh